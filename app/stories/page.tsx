import LivingStories from '@/src/components/LivingStories';

export const metadata = {
  title: 'Stories',
  description: 'Tea origin stories, artist features, Sri Lankan culture and heritage pieces, and café updates from Ceylon Stories.',
};

export default function StoriesPage() {
  return <LivingStories />;
}
