# Design Implementation Plan: MenuItemCard

## Summary
- **Scope:** Component
- **Target:** `src/components/menu/MenuItemCard.tsx`
- **Winner:** Variant D — Inline Price
- **Key improvements over previous design:**
  - Name and price share the first content row (left-anchored name, right-anchored price)
  - Description field is now surfaced (was in DB but never shown on card)
  - Quantity sits directly under the name as a small editorial label
  - Footer row: prep time left, "Details →" right (fades to clay-warm on hover)
  - Add button hover reveal unchanged from current design

## Files to Create / Change
- [ ] `src/components/menu/MenuItemCard.tsx` — new component (Variant D)
- [ ] `src/components/menu/MenuPage.tsx` — page that mounts the card grid + filter bar
- [ ] `src/types/menu.ts` — MenuItem + MenuCategory types (port from old Next.js version)

## Component API

```tsx
interface MenuItemCardProps {
  imageUrl: string
  isVegetarian: boolean
  name: string
  price: number
  originalPrice: number | null
  quantity: string
  description: string          // NEW — show below quantity
  prepTimeInMinutes: number
  tag?: string
  onAdd: (rect: DOMRect) => void
  onCardClick?: () => void
}
```

## Implementation: MenuItemCard (Variant D)

```tsx
// Card shell
<motion.div
  className="relative flex flex-col w-full overflow-hidden rounded-lg
             border border-mahogany/10 bg-cream-paper text-mahogany shadow-ink group cursor-pointer"
  variants={cardVariants}    // initial: opacity 0, y 20 → animate: opacity 1, y 0
  whileHover={{ scale: 1.02, transition: { duration: 0.25 } }}
>
  {/* Image block — same as current: h-48, gradient, tag, veg dot, hover Add btn */}

  {/* Content */}
  <div className="flex flex-col flex-grow p-4">

    {/* Row 1: name left, price right */}
    <div className="flex items-start justify-between gap-2">
      <h3 className="font-display text-[18px] leading-[1.1] flex-1">{name}</h3>
      <div className="flex flex-col items-end flex-shrink-0">
        <span className="font-display text-lg font-light">Rs. {price}</span>
        {savings > 0 && (
          <span className="font-body text-[10px] line-through text-mahogany/40">
            Rs. {originalPrice}
          </span>
        )}
      </div>
    </div>

    {/* Quantity */}
    <p className="mt-0.5 font-editorial text-[8px] tracking-[0.18em] uppercase text-mahogany/45">
      {quantity}
    </p>

    {/* Description */}
    <p className="mt-2 font-body text-[13px] leading-[1.55] text-mahogany/55 line-clamp-2">
      {description}
    </p>

    {/* Footer */}
    <div className="flex items-center justify-between mt-auto pt-3">
      <div className="flex items-center gap-1 font-editorial text-[9px] tracking-[0.1em] uppercase text-mahogany/45">
        <Clock className="w-3 h-3" />
        <span>{prepTimeInMinutes} min</span>
      </div>
      {onCardClick && (
        <span className="font-editorial text-[8px] tracking-[0.15em] uppercase
                         text-mahogany/35 group-hover:text-clay-warm transition-colors duration-300">
          Details →
        </span>
      )}
    </div>

  </div>
</motion.div>
```

## Required States
- [ ] **Default** — card at rest
- [ ] **Hover** — scale 1.02, image scales 1.05, Add button slides up from bottom
- [ ] **Loading skeleton** — `h-72 bg-mahogany/5 animate-pulse rounded`
- [ ] **Empty state** — "No items in this category yet"
- [ ] **Error state** — "Failed to load menu — tap to retry"

## Accessibility Checklist
- [ ] `<img>` has descriptive `alt={name}`
- [ ] Add button has `aria-label="Add {name} to order"`
- [ ] Veg/non-veg dot has `aria-label`
- [ ] Card is keyboard focusable when `onCardClick` is provided
- [ ] Focus ring: `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-mahogany focus-visible:ring-offset-2`
- [ ] `prefers-reduced-motion` — disable scale/translate animations

## Design Tokens in Use
All from `src/index.css @theme`:
- `bg-cream-paper` — card background
- `text-mahogany` — primary text
- `text-mahogany/55` — description muted text
- `text-mahogany/45` — meta text (quantity, time)
- `border-mahogany/10` — card border
- `text-clay-warm` — "Details →" hover, savings accent
- `text-sage-deep` — vegetarian indicator
- `font-display` — name, price
- `font-editorial` — labels, tags
- `font-body` — description

---
*Generated by Design Lab · Winner: Variant D*
